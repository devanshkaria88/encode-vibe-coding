<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-20T01:42:38Z

Replaced the bare R3F scene with a real Mapbox 3D London map + deck.gl overlay (user request). Rewrote scene specs
func 10-14: Mapbox Standard 3D buildings (pitched, react-map-gl) + deck.gl 3D value columns per slot (lock to winner
on clearing) + arc BidEdges from each agent's fixed AgentAnchor. Template 3D stack swapped R3F -> mapbox-gl +
react-map-gl + deck.gl. Added test req: scene tests mock mapbox-gl/deck.gl (no jsdom WebGL). New concept AgentAnchor.
Dry-run clean 19/19. Re-render: codeplain arbiter-app.plain --render-from 10. REQUIRES a free Mapbox public token
(VITE_MAPBOX_TOKEN=pk... in dist/.env.local) or tiles won't load; restart npm run dev after editing .env.local.
