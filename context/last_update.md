<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-19T19:57:48Z

Diagnosed and fixed the conformance-test infinite loop (renderer stuck on arbiter-core functionality 1, attempt 14+).
Root cause: run_conformance_tests_node.sh ran Vitest with --root on the build folder, so Vitest used the build's
config and never discovered the external conformance tests ("No test files found"). The renderer's own memory file
(arbiter-core-vitest-conformance-discovery-failure-v5.json) confirmed it. Because the test could never be SEEN, the
renderer kept regenerating a perfectly good test forever.
Fix (test_scripts/run_conformance_tests_node.sh only — not generated code): copy the read-only conformance folder
INTO the staged build (.conformance_run/), then run `npx vitest run --config <that folder>/vitest.config.ts` from the
build dir, so the config resolves vitest/config from ./node_modules, its process.cwd() alias points at the build's
src/core, and its __dirname include finds the test. Verified end-to-end: script exits 0, 10/10 tests pass.
Action: stop the current (doomed) render, then re-run `codeplain arbiter-app.plain` — it resumes from cache and
functionality 1 conformance now passes.
