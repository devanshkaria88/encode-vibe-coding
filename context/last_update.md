<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-19T19:23:49Z

Fixed the Arbiter specs after the first `codeplain arbiter-app.plain` failed with "maximum recursion depth exceeded".
Bisected with --dry-run. Three fixes: (1) concept cycle — :AdvertiserAgent: referenced :Referee: (a later concept that
points back), closing a cycle the requires-loader recursed on; reworded. (2) template linked `../test_scripts/...` which
escapes the module dir — dropped the links (scripts stay wired via config.yaml). (3) removed predefined :plain*: tokens
from spec prose. Both modules now pass --dry-run cleanly: arbiter-core 8/8 functionalities, arbiter-app 16/16, no cycles,
no complexity flags. Ready for a real render: `codeplain arbiter-app.plain`.
