<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-20T03:37:44Z

✅ DONE — arbiter-app fully rendered (19/19) and verified. Overnight render supervision complete.

WHAT WORKS (verified):
- Render completed successfully; all conformance tests green (core 8/8 + app 19/19), final regression sweep passed.
- 78/78 unit/component tests pass (npx vitest run in dist/).
- vite build produces a deployable bundle incl. the 3D Mapbox + deck.gl scene.
- App.tsx is fully WIRED (no longer a scaffold): Start -> fetch London inventory -> 4 agents -> orchestrator
  with round listener -> deck.gl scene/HUD -> ClearingPulse + ScorecardPanels.

TO RUN IT (you must add keys):
- Create dist/.env.local:
    VITE_MAPBOX_TOKEN=pk.<your mapbox public token>     # REQUIRED — 3D London map won't load without it
    VITE_ANTHROPIC_API_KEY=sk-ant-<key>                 # OPTIONAL — live Claude agents; without it agents bid deterministically
- Then: cd dist && npm install && npm run dev   (restart if the old dev server is still up), open http://localhost:5173/

KNOWN CAVEAT (non-blocking):
- `npm run build` fails at the tsc step (tsconfig missing "jsx") — the template fix didn't bake in because
  --render-from 18 didn't regenerate func 1's tsconfig. Use `npx vite build` (works; Vercel's default) or `npm run dev`.
  A full re-render (codeplain arbiter-app.plain --render-from 1) would bake the tsconfig jsx fix — optional.

OPTIONAL VISUAL POLISH (renderer's func-19 ambiguity note — app still passed, these are quality refinements):
- Agent IDs should be agent-1..agent-4 to match AGENT_ANCHORS in src/scene/constants.ts (so bid arcs anchor correctly).
- The wiring should identify the specific bidding agent per slot in each RoundEvent (valid BidEdge arc source, not placeholders).
- The start handler should be non-blocking so the UI shows 'In Progress' before the auction resolves (avoids start->finish jump).
- These can be addressed by clarifying func 19 and re-rendering --render-from 19. Ask Claude to do it.

Render journey (commits): scene rewrite to Mapbox/deck.gl, 6/18 conflict fixed definitively, AgentRuntime mock recipe,
supervised headless render with auto-retry on transient API 500s. No generated code was hand-edited.
