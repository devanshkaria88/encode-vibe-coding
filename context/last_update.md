<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-19T23:08:49Z

Ran the built app (dist/): 78/78 tests pass, vite build produces a deployable bundle. Found the app was a SCAFFOLD —
App.tsx Start button only console.logged; nothing wired inventory→agents→orchestrator→scene/scorecards. Also the Claude
path was unusable in-browser (stale model, no env key, CORS) and npm run build's tsc step failed (tsconfig missing jsx).
Fixes (specs only, appended so re-render is cheap): added 3 app functional specs (17: orchestrator emits RoundEvent per
round; 18: browser-hardened Claude :AgentRuntime: — VITE_ANTHROPIC_API_KEY, model claude-sonnet-4-6,
anthropic-dangerous-direct-browser-access header, deterministic valuation fallback that runs with NO key; 19: wire the
live auction into App). Added tsconfig jsx impl req to the React template. App now 19 functionalities; dry-run clean.
Re-render: codeplain arbiter-app.plain --render-from 17. Keys: none needed for build/tests/fallback demo; live Claude
needs VITE_ANTHROPIC_API_KEY in .env.local.
