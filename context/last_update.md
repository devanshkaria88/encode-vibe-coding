<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-19T19:17:44Z

Done. The full `forge-plain` workflow ran autonomously to completion — specs only, `codeplain` never invoked (per your instruction, so the Phase 4 dry-run was deliberately skipped in favor of structural self-validation).

## `.plain` files produced

| File | Role |
|---|---|
| `template/node-testing-base.plain` | Import module — shared TypeScript + Vitest reqs; links both Node test scripts from **one** place (satisfies the single-reference rule across both modules) |
| `template/typescript-react-app-template.plain` | Import module — imports node-testing-base, adds React 18 + Vite + R3F |
| `arbiter-core.plain` | Pure deterministic clearing engine + referee. No LLM/network/UI. Built first; exports its data + engine concepts |
| `arbiter-app.plain` | `requires: arbiter-core`, imports the React template. Inventory, agents, orchestrator, streaming, 3D scene, scorecard |

Plus: `test_scripts/run_unittests_node.sh`, `test_scripts/run_conformance_tests_node.sh`, `config.yaml`, `context/arbiter_assumptions.md`, `.gitignore` updated for generated artifacts.

## Module / requires structure
```
node-testing-base ──import──▶ typescript-react-app-template ──import──▶ arbiter-app
                  └──────────import──────────────────────────────────▶ arbiter-core
                                              arbiter-core ──requires──▶ arbiter-app
```
All one stack (TypeScript/Node/Vitest), so `requires` is legal. Render target = top of chain: **`arbiter-app.plain`**.

## Specs split for the 200-LOC ceiling (self-assessed — `analyze-if-func-spec-too-complex` hits the codeplain API, which you asked me not to run)
- **core**: clearing → single-slot + all-slots; referee → FairPrice/overpayment + surplus/leftOnTable + concessionErrors/scorecard
- **app**: InventorySource → fetch + cache; AgentRuntime → contract/budget-guard + Claude bidder; Orchestrator → round-loop + scoring; LondonMapScene → canvas + nodes; ScorecardPanel → display + click-to-explain

## Exact-number acceptance scenarios (pin the maths)
- **S1** overpay → winner A @ **140**, FairPrice **130**, overpayment **10**, surplus **40**, overbid concession
- **S2** premature drop → C leftOnTable **60**, premature-drop concession
- **S3** uncontested → clearingPrice = basePrice **100**, overpayment **0**
- Over-budget log (budget 120, bid 130) → validator rejects naming agent/130/120

## Gaps I filled with defaults
All logged in **`context/arbiter_assumptions.md`**. The notable ones:
- Created `typescript-react-app-template` **locally** (no bundled template of that name exists) + added a `node-testing-base` template to keep script links single-sourced.
- **No `prepare_environment`** → conformance is install-inline (small-N justification); Bash scripts only (macOS).
- **Claude model default `claude-sonnet-4-6`** (latency/cost across many per-round calls), env-overridable, key from `ANTHROPIC_API_KEY`.
- PricingModel formula, London bbox, Overpass endpoint, cache path, 4 agents / ~6 slots, orchestrator depends on the `:AgentRuntime:` contract so tests inject a deterministic bidder while production stays live.

Validation passed: every line ≤120 chars, no concept collisions between modules, all referenced concepts are defined/exported/predefined, import & requires graph resolves to real files, no unsatisfied `required_concepts`.

Committed as `eddcc16`; `context/last_update.md` updated with `agent: claude`.

## To render it yourself
```
codeplain arbiter-app.plain
```
Side-channel scripts that were generated (run from repo root):
```
./test_scripts/run_unittests_node.sh plain_modules/arbiter-core
./test_scripts/run_conformance_tests_node.sh plain_modules/arbiter-core conformance_tests/arbiter-core/<spec>
```
(same two scripts apply to `arbiter-app` build folders — they're language-generic over `$1`).

One thing to watch on your first render: the conformance script's Node module resolution between the external tests folder and the staged build is the most likely spot to need a tweak in `***test reqs***` — flagged in the assumptions file.
