<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-19T21:28:36Z

Fixed the arbiter-app functionality 8 conformance loop. Root cause was NOT the harness this time — it was a real
orchestrator bug from an under-specified round model in functionality 7. The generated runRound treated "one agent has
bid" as "slot settled", so after the first bid every other agent was skipped forever — every auction collapsed to a
single uncontested bidder (clearingPrice=basePrice), so the func-8 integration test's contested numbers could never match
and the renderer thrashed referee.ts/orchestrator.ts (also breaking unit tests).
Fix (arbiter-app.plain specs only): rewrote functionality 7 to pin the round model — agents are contenders until they
drop (not "until they bid"), a slot stays contested while 2+ contenders remain, and within a round agents act one at a
time in fixed order seeing same-round bids (sequential-live). Reframed functionality 8's acceptance test to an invariant
(bundle scorecards == Referee on the bundle's own log) instead of hardcoded emergent amounts (those are pinned in
arbiter-core's fixed-log tests). Dry-run clean, app 16/16. Resume with: codeplain arbiter-app.plain --render-from 7.
