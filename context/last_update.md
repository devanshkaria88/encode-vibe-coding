<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-19T19:16:15Z

Authored the initial Arbiter `***plain` specs via the forge-plain workflow (specs only — no app code, codeplain not run).

Modules (requires-chain, all TypeScript / Node / Vitest):
- template/node-testing-base.plain — shared TS + Vitest reqs; links the two Node test scripts once.
- template/typescript-react-app-template.plain — imports node-testing-base; React 18 + Vite + R3F.
- arbiter-core.plain — pure deterministic clearing engine + referee; built first; exports its data/engine concepts.
- arbiter-app.plain — requires: arbiter-core, imports the React template; inventory, agents, orchestrator, streaming, 3D, scorecard.

Also: test_scripts/run_unittests_node.sh, test_scripts/run_conformance_tests_node.sh, config.yaml, context/arbiter_assumptions.md.
Exact-number acceptance scenarios S1/S2/S3 + over-budget validation pin the core maths. See context/arbiter_assumptions.md for every default I filled. Render target: `codeplain arbiter-app.plain`.
