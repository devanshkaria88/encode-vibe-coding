<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-06-20T09:01:37Z

The statusline-setup agent looked through your shell config but couldn't find a custom prompt to base a status line on — your `.zshrc` is minimal and doesn't define a `PS1`.

To set up your status line, just tell me what you'd like it to show. Common options:

- **Current directory** (e.g. `~/hackathonProjects/encode-vibe-coding`)
- **Git branch** (e.g. `main`)
- **Claude model name** (e.g. `Opus 4.8`)
- **Username / hostname**
- **Context usage / cost**

Or, if you do have a custom prompt you like elsewhere, paste your `PS1` value and I'll translate it into a status line. What would you like?
